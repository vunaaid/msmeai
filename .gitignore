# Cache và logs hệ thống
.cache/
.claude/
.claude.json
.bash_history
.bash_logout
.bashrc
.profile
.sudo_as_admin_successful
.wget-hsts
.dotnet/
.kube/
.local/
.copilot/
.vscode-server/
.ssh/
.config/

# Files nhạy cảm
pwd.txt
token.txt
*.credentials*
copy-ssh-key.sh

# Secrets — toàn bộ thông tin tài khoản, mật khẩu, API key, .env thật
/secrets/

# Git
*.orig

# Node / pnpm
node_modules
.pnpm-store
.turbo

# Python (services/whisper, …)
.venv/
venv/
__pycache__/
*.pyc

# Build
.next
dist

# Env
.env
.env.local
.env.*.local
!.env.example

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Docker data (dev)
docker/postgres-data
docker/redis-data
docker/minio-data

# Local runtime data & backups — NEVER commit (added for public release)
/backups/
/data/
/logs/
*.sql
*.dump

# Prisma generated client — regenerated by 'prisma generate' (postinstall)
packages/db/generated/
